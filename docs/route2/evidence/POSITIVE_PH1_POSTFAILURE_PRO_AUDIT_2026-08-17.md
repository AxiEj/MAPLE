# Positive-parent PH1.0 post-failure Pro audit — 2026-08-17

## Verified external review

The retained Windows Chrome surface was checked before submission:

```text
composer = Pro
power    = Pro, 5 of 5.
```

Generation was observed and allowed to finish before the complete response
was copied.

```text
conversation:
  https://chatgpt.com/c/6a823928-7274-83e8-b998-d35b2a50fd27
conversation control:
  conversation-options-WEB:692c48a8-080f-4123-8983-7e9a72395d00
prompt SHA256:
  aadf1951163d7a9bc169aac268da788d26b95110d59d3a0721c9569c5ef4d4a4
submission evidence SHA256:
  95578ed08a760b90e1cf0b080b3081db91d9bc3016f7f35dab8ee76670d9249e
answer SHA256:
  d6c85d4ad3298d5e0fd300eb744e125e0bc7c4245530564387de547cfd99ae50
answer marker:
  MOVE TO HETEROGENEOUS ELECTROSTATICS AUDIT
```

No private checkpoint data or confirmation values were submitted.

## Critically accepted decision

The answer agrees with the local preregistered result: `1.6969972` cannot be
called a pass against a frozen `<=1.5` rule.  The mixed-505 pass cannot rescue
the failed water conjunct, and no additional CDS candidate is allowed.

The hybrid route itself remains scientifically open because the earlier
harmonic comparison did not isolate one physical change: the successful
PCMSolver pilot used point MDP permanent moments plus Gaussian POLAR induced
increments, while the adverse harmonic experiment may have applied common
Gaussian source semantics.  Correcting that category error is an upstream
electrostatic-model change, not another CDS fit.

## Accepted finite decision tree

1. **Analytic modal identities.**  On single spherical dielectric interfaces,
   verify harmonic normalization, radius powers, dielectric placement,
   Gaussian width meaning, sign, units, and half-coupling for every implemented
   degree.  Modal relative error must be at most `1e-10`; reaction-energy
   relative error must be at most `1e-8` with an absolute
   `1e-8 kcal/mol` floor.
2. **Shared-cavity frozen-source equivalence.**  Compare PCMSolver and harmonic
   calculations separately for permanent point moments, induced 1.5-A Gaussian
   increments, and their direct sum.  Neither solver may regenerate the induced
   source.  Per component and row, require agreement within both
   `max(5*(eps_H+eps_PCM), 0.002)` and `0.02 kcal/mol`; scaled RMS reaction
   potential/field discrepancy must be at most `1e-3`.
3. **Smooth scalar gate.**  The production harmonic cavity must keep fixed
   dimension, weighted adjointness, effective-operator reciprocity defect at
   most `1e-8`, rotation-energy error at most `1e-8 kcal/mol`, scaled force
   covariance at most `1e-6`, and scalar-VJP finite-difference error at most
   `1e-4 kcal mol^-1 A^-1`.
4. **First-shell discrimination.**  Only after continuum equivalence passes may
   a target-blind distance-resolved solute-water component panel test for
   genuine short-range physics.  Long-range failure remains a source problem;
   a localized noncontinuum remainder must not be hidden in CDS.
5. **One final profile.**  Only after the target-independent gates pass may one
   newly preregistered heterogeneous-electrostatic profile replace the
   electrostatic component while retaining the already frozen PH1 OOF CDS
   contribution.  No CDS refit is allowed.  Both original `<=1.5` gates remain.

This review is advisory.  The repository implementation, analytic identities,
source-block comparisons, and exact preregistration remain authoritative.
