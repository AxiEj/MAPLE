# Hybrid ddX source/ledger Pro audit (2026-08-17)

This directory freezes two non-overlapping mathematical reviews performed in
the retained Chrome conversation with the genuine **Pro** model.  Immediately
before each submission the model selector displayed `Pro, 5 of 5`; generation
showed `Pro thinking`.  The two complete answers contain 71,614 characters.

The reviews used no experimental hydration targets.  They were requested only
after local field-semantics and source/continuum evidence had narrowed the
remaining ambiguity to a hard nonuniform-work/energy-ledger question.

## Invocation identity

| review | verified UTC | submitted UTC | prompt SHA-256 | answer SHA-256 |
| --- | --- | --- | --- | --- |
| Q15 | `2026-08-17T00:26:20.5290403Z` | `2026-08-17T00:26:21.4306581Z` | `a7d5214f66b93e6a74e2e7f486f91acc68e5f941f9cf4d95c04e748cf7453ca4` | `9dbef6f78a632715d5722c7e570cb26cf3300bc8d4294af6980f462497b1bf76` |
| Q16 | `2026-08-17T00:54:11.4987816Z` | `2026-08-17T00:54:12.6651654Z` | `2d10af15ba0b7e9ff1e8bf0b2ffdcce12556e34d95d84069bea250207861214c` | `bf96f75ee064add3296d35ad944ca457d582b814f88eb8c8aeb79402a9fa72b4` |

Both submissions used Chrome window `Mathematical Diagnosis of MLIP Model -
Google Chrome` and conversation control
`conversation-options-WEB:d5e43b8a-882e-43e9-81a5-21f521654d1b`.

## Accepted conclusions

1. The passed upstream `+E dot mu` identity fixes only the three-dimensional
   uniform-electric-field subspace.  It does not define the work of a general
   nonuniform ddPCM reaction potential in the native eight-channel space.
2. No endpoint expression such as `<c_p + delta c_g, u_model>` is currently
   type-correct or physically admitted.  A valid native factorization would
   require declared maps satisfying `L* D_p = B_p` and `L* D_g = B_g`, including
   the actual metrics and gauge.
3. `<grad_u E_raw(u), u>` is a directional derivative, not in general the
   energy difference `E_raw(u)-E_raw(0)`.  The latter is the line integral of
   the same-graph gradient.
4. From already implemented scalar objects, the only honest ledgers are
   `Phi0 = E_vac + G_cont` and
   `Phi_raw = E_conditioned_raw(u*) + G_cont`.  Both are operational scalars;
   neither is thereby a unique physical solvation free energy or Tier V.
5. The current checkpoint route is not dead.  A chosen operational scalar can
   have a conservative block-implicit-adjoint force on a single differentiable
   root branch.  What is closed is the claim that the present evidence uniquely
   identifies a common physical nonuniform-work functional.
6. The next target-independent experiment is the direct-sum ddPCM half-work and
   nonuniform-work replay.  No development solvation target should be opened to
   decide this mathematical question.

## Local evidence that sharpens the Pro answer

The installed upstream MACE-POLAR implementation exposes a uniform
`external_field` three-vector.  It constructs
`external_potential = [0, external_field]`, adds half of that uniform field to
each spin channel, and adds the explicit scalar
`external_potential[:,1:] * total_dipole` to the energy.  MAPLE's native-field
injection supplies arbitrary eight-channel features through the projector while
leaving that upstream uniform field zero.  There is therefore no official
arbitrary-nonuniform explicit-work API to replay for this checkpoint.

This selects Q16 **Outcome C**: the uniform explicit-work identity is a special
branch identity; it must not be extrapolated into an ad hoc nonuniform endpoint
term.  The full source-to-ddPCM energy-dual half-work remains testable and is the
next local audit.

## Claim boundary

This evidence is an external mathematical second opinion, not implementation
or admission evidence.  It opens no `E/F/H/V/M` capability, does not select
`Phi0` or `Phi_raw` by chemical accuracy, does not validate MACE-MDP moments as
a quantitative PCM source, and does not use experimental solvation data.
