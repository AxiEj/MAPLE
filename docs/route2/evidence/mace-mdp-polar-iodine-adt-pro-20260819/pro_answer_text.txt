Show more
Your message actions
Copy message
Share prompt
Edit message
Worked for 26m 32s
Decision
Select B as the first operational iodine extension, with D as a mandatory target-free audit and reference standard.
 B must be registered strictly as an 
ECP-valence ADT translation shape
. It must not masquerade as, alias, or automatically supply a full-neutral-atom penetration density.
The iodine record remains blocked until B passes the frozen B-versus-D operator gate below. If it fails, the scientifically valid fallback is to use D itself for iodine or retain E and report the 504-record supported domain. Do not relax the gate after seeing the comparison.
GO/NO-GO adjudication
Option
Verdict
Role and reason
A
NO-GO
A converged 53-electron SCF solution in an ECP-valence basis is not a defensible all-electron iodine density. Convergence only establishes a stationary solution in the supplied one-particle space; it does not repair the absence of a balanced core basis or relativistic treatment.
B
GO, selected
Best first operational ADT extension, provided it is capability-separated and passes the D audit. Use N_{\mathrm{eff}}=25 , effective fixed core +25 , and no full-neutral penetration capability.
N_{\mathrm{eff}}=25
+25
C
NO-GO
Exactly duplicates B’s ADT tangent and self-work in real arithmetic, while falsely relabeling a rescaled pseudo-density as a 53-electron full-atom density. It adds no ADT information and corrupts penetration semantics.
D
GO, mandatory reference
Best audit/reference standard and direct production fallback. Use a genuine all-electron relativistic basis, spherical average-of-configuration, finite nucleus, and picture-consistent density.
E
GO only as fail-closed fallback
Scientifically valid as an explicitly supported-domain 504-record result, but it does not solve iodine support.
Copy table
For iodine, 
def2-TZVPD
 is explicitly paired with 
def2-ECP
; the documented 
def2-ECP
 core size is 28 electrons for Rb–Xe, leaving 25 explicit electrons for 
Z=53
. ECP calculations use a valence-only basis and represent the omitted core through an effective operator, so removing the ECP while retaining that basis does not turn it into an all-electron basis. 
FACCTs +2 FACCTs +2
PySCF’s atomic spherical-average implementation explicitly performs angular averaging and fractional occupations, and its occupancy construction subtracts the ECP core shells when an ECP is present. Thus B is consistent with the semantics of the current atomic generator; A is not. 
PySCF +2 PySCF +2
1. Exact mathematical relation between B and C
Let the B pseudo-density be 
n_B
, with
N_B=\int n_B(\mathbf r)\,d\mathbf r=25,
and define C by uniform scaling
c=\frac{53}{25},\qquad n_C=c\,n_B,\qquad N_C=cN_B=53.
Define the normalized cumulative charge fraction
F_X(r)=\frac{N_X(<r)}{N_X}, \qquad N_X(<r)=4\pi\int_0^r t^2 n_X(t)\,dt.
Because 
N_C(<r)=cN_B(<r)
,
F_C(r) = \frac{cN_B(<r)}{cN_B} = F_B(r).
Therefore, for every surface point 
\mathbf s
, atomic center 
\mathbf R_A
, and prescribed dipole 
\mathbf p_A
,
V_C(\mathbf s) = \mathbf p_A\cdot(\mathbf s-\mathbf R_A) \frac{F_C(r)}{r^3} = \mathbf p_A\cdot(\mathbf s-\mathbf R_A) \frac{F_B(r)}{r^3} = V_B(\mathbf s).
Thus B and C have 
exactly the same finite-density ADT 
\phi
 tangent
.
Self-work invariance
Write the Gaussian expansion coefficients as 
N_a
, so C has 
N'_a=cN_a
. Then
\begin{aligned} \kappa_C &= \frac{4}{3\sqrt{\pi}(cN)^2} \sum_{ab} (cN_a)(cN_b) \left[ \frac{\alpha_a\alpha_b}{\alpha_a+\alpha_b} \right]^{3/2} \\ &= \frac{4}{3\sqrt{\pi}N^2} \sum_{ab} N_aN_b \left[ \frac{\alpha_a\alpha_b}{\alpha_a+\alpha_b} \right]^{3/2} \\ &=\kappa_B. \end{aligned}
Hence the minimum-work allocation is also identical. In the isotropic case,
W=\frac12\sum_A\kappa_A|\mathbf p_A|^2, \qquad \sum_A\mathbf p_A=\mathbf P,
gives
\mathbf p_A = w_A\mathbf P, \qquad w_A= \frac{\kappa_A^{-1}} {\sum_B\kappa_B^{-1}}.
Because B and C have the same 
\kappa_I
, they have the same iodine allocation weight and therefore the same complete ADT 
\phi/\psi
 source operator.
Translation-tangent identity
The nominal displacements differ:
\mathbf d_B=-\frac{\mathbf p}{25}, \qquad \mathbf d_C=-\frac{\mathbf p}{53} =\frac{1}{c}\mathbf d_B.
But the first-order translated-density tangent is unchanged:
\mathbf d_C\cdot\nabla n_C = \frac{\mathbf d_B}{c}\cdot\nabla(cn_B) = \mathbf d_B\cdot\nabla n_B.
So B and C are not merely close: 
they are the same ADT tangent under a redundant reparameterization
.
This identity is exact in real arithmetic. Independent serialization, refitting, thresholding, or floating-point rescaling need not be bitwise identical. If tested in software, C should be formed as an algebraic alias of B rather than independently refitted.
2. Why B and C are not equivalent penetration densities
Let 
\mathcal V[n](r)
 denote the positive Coulomb potential generated by the electron number density 
n
. The coherent ECP-neutral electrostatic primitive corresponding to B would be
P_B^{\mathrm{ECP}}(r) = \frac{25}{r}-\mathcal V[n_B](r).
C instead gives
\begin{aligned} P_C(r) &= \frac{53}{r} - \mathcal V\!\left[\frac{53}{25}n_B\right](r) \\ &= \frac{53}{25} \left[ \frac{25}{r}-\mathcal V[n_B](r) \right] \\ &= \frac{53}{25}P_B^{\mathrm{ECP}}(r). \end{aligned}
Therefore C’s penetration potential is not equal to B’s except where the latter happens to vanish. More importantly, it is not the all-electron neutral iodine potential
P_D(r) = \frac{53}{r}-\mathcal V[n_D](r), \qquad \int n_D=53,
because generally
n_D(r)\ne \frac{53}{25}n_B(r).
C has replaced the missing 28-electron core density by an artificial uniform amplification of the valence pseudo-density. That changes both the inner radial structure and the amount of valence penetration by a factor 
53/25
.
Consequently:
B is a legitimate ADT-only effective pseudoatom.
B is a legitimate 
ADT-only effective pseudoatom
.
C is a misleading relabeling of B.
C is a misleading relabeling of B.
Neither B nor C may populate the existing neutral_full_atom_penetration role.
Neither B nor C may populate the existing 
neutral_full_atom_penetration
 role.
A request for iodine full-neutral penetration must fail closed until a D-derived asset exists.
A request for iodine full-neutral penetration must fail closed until a D-derived asset exists.
This capability split is essential. It is not acceptable to place one density blob behind two mathematically different interfaces merely because both previously happened to require 
N=Z
.
3. Correct ECP bookkeeping for B
For iodine with ECP28:
Z_{\mathrm{nuclear}}=53, \qquad N_{\mathrm{core,removed}}=28,
so
Q_{\mathrm{core,eff}}=53-28=+25, \qquad N_{\mathrm{explicit}}=25.
The classical pseudoatom charge distribution used for the translation argument is
\rho_{\mathbf d}(\mathbf r) = 25\,\delta(\mathbf r) - n_v(\mathbf r-\mathbf d),
where
\int n_v(\mathbf r)\,d\mathbf r=25.
For a centered spherical density,
\int \mathbf r\,n_v(\mathbf r)\,d\mathbf r=0.
Its dipole is therefore
\begin{aligned} \boldsymbol\mu &= \int \mathbf r\,\rho_{\mathbf d}(\mathbf r)\,d\mathbf r \\ &= -\int \mathbf r\,n_v(\mathbf r-\mathbf d)\,d\mathbf r \\ &= -25\mathbf d. \end{aligned}
Thus the prescribed dipole is obtained by
\boxed{\mathbf d=-\frac{\mathbf p}{25}}.
This result is exact for the dipole moment of a translated centered density, not merely asymptotic to first order. The ADT construction subsequently retains the infinitesimal tangent because that is the registered linear translation model.
At large distance,
F_B(r)=\frac{N_B(<r)}{25}\longrightarrow 1,
and hence
V_B(\mathbf s) \longrightarrow \mathbf p\cdot \frac{\mathbf s-\mathbf R_I} {|\mathbf s-\mathbf R_I|^3}.
Therefore:
Point-core charge:.
Point-core charge:
+25e
.
Explicit electron count:.
Explicit electron count:
25
.
Translation displacement: in atomic units.
Translation displacement:
\mathbf d=-\mathbf p/25
 in atomic units.
Far-field  dipole: exactly , with no  or  factor.
Far-field 
\psi
 dipole:
 exactly 
\mathbf p
, with no 
25/53
 or 
53/25
 factor.
ADT normalization:.
ADT normalization:
N_{\mathrm{eff}}=25
.
Yes, translating only the 25 explicit pseudo-electrons relative to the fixed effective 
+25
 core produces the requested long-range dipole exactly.
The phrase “point core 
+25
” applies to the classical electrostatic accounting used by this density representation. It does 
not
 mean that the electronic-structure calculation should replace the nonlocal def2-ECP Hamiltonian by a bare Coulomb 
+25/r
 potential. The full ECP remains in the atomic SCF calculation.
Using effective atomic numbers and valence-only densities is established ECP bookkeeping in electron-density force-field constructions. That literature supports B as a physically coherent ECP representation, but it does not by itself prove that B reproduces this particular ADT self-work and surface damping; that is why D remains mandatory. 
ACS Publications +2 AIP Publishing +2
What B approximates
B represents the entire requested atomic dipole through an effective valence translation. It does 
not
 claim that:
real iodine core electrons have zero response;
real iodine core electrons have zero response;
the ECP pseudo-density is the physical all-electron density near the nucleus;
the ECP pseudo-density is the physical all-electron density near the nucleus;
its short-range penetration or self-work must equal an all-electron result.
its short-range penetration or self-work must equal an all-electron result.
The far-field moment is protected exactly. The finite-
r
\phi
 damping and 
\kappa_I
 are not. In particular, 
\kappa
 can be sensitive to pseudoization in the inner region even when the cavity potential is nearly unchanged. That is the principal physical risk being audited against D.
4. Minimum requirements for D
D should be generated as a separate, content-addressed all-electron reference. A defensible minimum is as follows.
4.1 Spherical atomic state
Use a neutral-iodine average-of-configuration HF construction, not one oriented open-shell determinant. For spin-free iodine, the 
5p^5
 occupation must be fractionally averaged over the degenerate 
p
 angular components so that the scalar density is spherical. This matches the conceptual convention of the existing 
AtomSphAverageRHF
 assets, whose implementation explicitly angular-averages the Fock and overlap blocks and applies spherical fractional occupancies. 
PySCF +1
Record and hash:
atomic configuration;
atomic configuration;
fractional occupations;
fractional occupations;
spin/state convention;
spin/state convention;
SCF convergence thresholds;
SCF convergence thresholds;
radial integration settings.
radial integration settings.
4.2 Genuine all-electron relativistic basis
Use an all-electron basis designed for the selected relativistic Hamiltonian, for example:
x2c-TZVPPall followed by x2c-QZVPPall; or
x2c-TZVPPall
 followed by 
x2c-QZVPPall
; or
a diffuse Dyall sequence such as dyall.av3z followed by dyall.av4z.
a diffuse Dyall sequence such as 
dyall.av3z
 followed by 
dyall.av4z
.
The tail matters because the density is sampled at cavity radii. At least one basis-convergence comparison must therefore include sufficient diffuse flexibility; total atomic energy convergence alone is not the acceptance quantity.
The Karlsruhe X2C families are explicitly all-electron through Rn and have separate basis variants intended for spin-orbit two-component calculations. 
FACCTs +1
4.3 Finite nucleus and kinetic balance
Use a specified finite-nucleus model, preferably for 
^{127}\mathrm I
, and bind the model and parameters. A point nucleus combined with a large relativistic all-electron basis can distort the core density or invite numerical pathologies. The finite-nucleus choice is especially relevant here because 
\kappa
 samples the inner density much more strongly than the cavity potential does. Relativistic software documentation explicitly recommends a finite-nucleus treatment for such all-electron calculations. 
ORCA Manual +1
The four-component or X2C implementation must use its proper kinetic-balance construction. Do not manually combine unrelated large- and small-component bases.
4.4 Picture-consistent electron density
The radial number density is a property, not just an energy. Under X2C, the local density operator
\hat n(\mathbf r)=\sum_i\delta(\mathbf r-\mathbf r_i)
must be represented in the same picture as the X2C wavefunction. Either:
reconstruct/back-transform the corresponding four-component density, including large- and small-component contributions; or
reconstruct/back-transform the corresponding four-component density, including large- and small-component contributions; or
apply the appropriate X2C picture-change transformation to the local density operator.
apply the appropriate X2C picture-change transformation to the local density operator.
Simply contracting an X2C density matrix with untransformed nonrelativistic AO products is not a sufficient reference procedure. Picture change exists precisely because the transformed wavefunction and property operator must inhabit the same representation. 
ORCA Manual +2 ORCA Manual +2
The parent picture-corrected density must integrate to 53 before fitting. Do not silently repair a deficient parent density by post hoc normalization.
4.5 Two-electron relativistic effects
A one-electron SF-X2C Hamiltonian does not automatically provide all two-electron relativistic picture-change effects. For a cavity-tail density these may be small, but 
\kappa
 is core-sensitive. Therefore perform at least one independent higher-level check using either:
four-component Dirac–Coulomb AOC-HF; or
four-component Dirac–Coulomb AOC-HF; or
an atomic-/extended-atomic-mean-field X2C treatment carrying the relevant two-electron picture-change corrections.
an atomic-/extended-atomic-mean-field X2C treatment carrying the relevant two-electron picture-change corrections.
Such X2C corrections are specifically designed to reproduce four-component mean-field properties while retaining a two-component representation. 
AIP Publishing +1
4.6 Is spin-free X2C enough?
Spin-free X2C is sufficient as the primary spherical PCM-cavity reference only after a mandatory spin-orbit sensitivity test.
Spherical averaging removes angular anisotropy. It does not prove that spin-orbit coupling leaves the radial density unchanged: the 
p_{1/2}
 and 
p_{3/2}
 radial functions can differ. Therefore there is no mathematical basis for simply declaring SO irrelevant for iodine.
The appropriate protocol is:
generate the primary D density with spin-free X2C AOC-HF;
generate the primary D density with spin-free X2C AOC-HF;
generate one two-component spin-orbit or four-component AOC density, spherically averaged over the relevant  ensemble;
generate one two-component spin-orbit or four-component AOC density, spherically averaged over the relevant 
M_J
 ensemble;
compare the actual cavity kernels and ADT operator using the gate below;
compare the actual cavity kernels and ADT operator using the gate below;
retain spin-free D if it passes;
retain spin-free D if it passes;
otherwise make the SO density the D reference and production fallback.
otherwise make the SO density the D reference and production fallback.
Thus 
two-component SO is mandatory as an audit calculation, but not automatically mandatory as the retained radial asset
.
A code must genuinely support the requested two-component or four-component SCF. For example, ORCA’s documented X2C implementation is currently the scalar spin-free 
SF-X2C-1e
 form, while its 
-2c
 basis variants are intended for codes that support spin-orbit X2C. 
ORCA Manual +2 FACCTs +2
5. Finite target-free B-versus-D validation gate
This gate uses no experimental value, no solvation energy, and no model residual. It certifies the atomic radial representation and its actual propagation through the frozen ADT/ddPCM operator.
5.1 Two observables derived from D
From the same all-electron D calculation, retain:
D_{53}: n_D^{\mathrm{all}}(r),\qquad \int n_D^{\mathrm{all}}=53.
Also form an ECP-matched outer-space diagnostic
D_{25}: n_D^{\mathrm{outer}}(r),\qquad \int n_D^{\mathrm{outer}}=25,
by excluding precisely the 28 shells represented by ECP28:
1s^2\,2s^2\,2p^6\,3s^2\,3p^6\,3d^{10}.
The remaining explicitly represented shell space is
4s^2\,4p^6\,4d^{10}\,5s^2\,5p^5.
This is not a fitted partition. It is the fixed orbital-space partition defined by the ECP core size.
Use:
 to test whether B is an adequate ECP-valence radial representation;
D_{25}
 to test whether B is an adequate ECP-valence radial representation;
 to test whether using that representation materially changes the canonical full-density ADT operator.
D_{53}
 to test whether using that representation materially changes the canonical full-density ADT operator.
5.2 Radial quantities
For 
X\in\{B,D_{25},D_{53}\}
, calculate
F_X(r)=\frac{4\pi}{N_X}\int_0^r t^2n_X(t)\,dt,
F_X'(r)=\frac{4\pi r^2n_X(r)}{N_X},
and the vector translation kernel
\mathbf k_X(\mathbf r) = F_X(r)\frac{\mathbf r}{r^3}, \qquad V_X=\mathbf p\cdot\mathbf k_X.
Its coordinate Jacobian is
\nabla_{\mathbf r}\mathbf k_X = \frac{F_X(r)}{r^3}I + \left[ \frac{F_X'(r)}{r^4} - \frac{3F_X(r)}{r^5} \right]\mathbf r\mathbf r^{\mathsf T}.
This Jacobian must be included because two radial shapes can give similar potentials while producing materially different coordinate derivatives.
5.3 Certified radial domain
Let
\mathcal R_I = \left\{ |\mathbf s_j-\mathbf R_I|: \mathbf s_j\text{ is an actually used, nonzero-weight ddPCM surface point} \right\}
for the frozen iodine geometry.
Augment it with:
the corresponding distances at  Å;
the corresponding distances at 
\pm0.02
 Å;
256 deterministic Chebyshev points overclipped to positive radius.
256 deterministic Chebyshev points over
[r_{\min}-0.10\ \text{\AA},\,r_{\max}+0.10\ \text{\AA}],
clipped to positive radius.
The registry certificate should state this radial validity interval. A later iodine execution whose actual surface distances fall outside it must fail closed or obtain a new audit; it must not silently extrapolate the certification.
5.4 Actual operator matrices
Using the three Cartesian unit molecular dipoles, construct the exact frozen-code matrices
T_{\phi,X}:\mathbb R^3\rightarrow\text{ddPCM }\phi\text{ block},
T_{\psi,X}:\mathbb R^3\rightarrow\text{ddPCM }\psi\text{ block},
including the minimum-work allocation over all atoms. Also construct
S_X=A^{-1}T_X
for the continuum state and
H_X=L\,A^{-1}T_X
for the returned native MACE-POLAR field. These are operator comparisons, not energy comparisons.
For a boundary mass or quadrature matrix 
M
, use
\epsilon_M(X,Y) = \frac{\|M^{1/2}(X-Y)\|_F} {\|M^{1/2}Y\|_F}.
Also use the induced spectral error
\epsilon_2(X,Y) = \frac{\|X-Y\|_2}{\|Y\|_2}
and the maximum row error
\epsilon_{\mathrm{row}} = \frac{\max_j\|\operatorname{row}_j(X-Y)\|_2} {\max_j\|\operatorname{row}_j(Y)\|_2}.
Do not use per-node relative errors, because legitimate zero or nearly zero potentials make those ratios ill-conditioned.
5.5 Hard acceptance thresholds
These are the numerical gates I would freeze before inspecting B-versus-D results.
Gate
Required result
D parent integrity
(\left
D basis convergence
Between the two highest-quality all-electron bases: (\max_{\mathcal R_I}
Picture-change/higher-level check
Picture-consistent X2C versus four-component or corrected-X2C: fixed-dipole \epsilon_M(T_\phi)\le10^{-3} , and continuum-returned-field error \epsilon_M(H)\le5\times10^{-3} . Otherwise the higher-level density becomes D.
\epsilon_M(T_\phi)\le10^{-3}
\epsilon_M(H)\le5\times10^{-3}
Spin-orbit sensitivity
Spin-free versus SO: fixed-dipole \epsilon_M(T_\phi)\le2\times10^{-3} ; full allocated T and H errors \le5\times10^{-3} ; (
\epsilon_M(T_\phi)\le2\times10^{-3}
T
H
\le5\times10^{-3}
Gaussian-fit certification
Positive coefficients and exponents; relative normalization error \le10^{-12} ; (\max_{\mathcal R_I}
\le10^{-12}
B versus D_{25} : radial ECP representation
D_{25}
RMS (
B versus D_{25} : portable self-work
D_{25}
For a generally reusable iodine asset, require (\left
B versus D_{53} : allocation impact
D_{53}
(
B versus D_{53} : full ADT surface operator
D_{53}
For both allocated \phi and \psi : \epsilon_M\le5\times10^{-3} , \epsilon_2\le10^{-2} , and \epsilon_{\mathrm{row}}\le2\times10^{-2} .
\phi
\psi
\epsilon_M\le5\times10^{-3}
\epsilon_2\le10^{-2}
\epsilon_{\mathrm{row}}\le2\times10^{-2}
Continuum-amplified difference
\epsilon_M(S_B,S_{D_{53}})\le10^{-2} and \epsilon_M(H_B,H_{D_{53}})\le10^{-2} .
\epsilon_M(S_B,S_{D_{53}})\le10^{-2}
\epsilon_M(H_B,H_{D_{53}})\le10^{-2}
Coordinate derivative
Relative Frobenius error in the complete coordinate Jacobian of T and H \le2\times10^{-2} , with the existing AD/FD consistency gate independently satisfied.
T
H
\le2\times10^{-2}
Fixed- \mathbf p_I \psi identity
\mathbf p_I
\psi
Before allocation, B and D must produce the same point-dipole \psi primitive to \le10^{-13} relative/absolute numerical tolerance; any larger difference is an implementation error.
\psi
\le10^{-13}
Copy table
The distinction in the last row matters: at fixed atomic 
\mathbf p_I
, the 
\psi
 primitive is exactly the same point dipole. After molecular minimum-work allocation, B and D may have different 
\psi
 blocks because their 
\kappa_I
 values may assign different atomic dipoles.
Fail-closed decision
If D itself fails basis, picture-change, or SO certification, iodine remains unsupported.
If D itself fails basis, picture-change, or SO certification, iodine remains unsupported.
If D passes and B passes every B-versus-D gate, admit B.
If D passes and B passes every B-versus-D gate, admit B.
If D passes but B fails, do not retune B or loosen thresholds. Use the D density as the iodine ADT shape, or report E.
If D passes but B fails, do not retune B or loosen thresholds. Use the D density as the iodine ADT shape, or report E.
No solvation prediction, experimental value, or target residual may enter this decision.
No solvation prediction, experimental value, or target residual may enter this decision.
6. Recommended implementation contract
The B registry entry should contain, at minimum:
Copy
Edit code
element                     = I
role                        = adt_translation_shape
representation              = ecp_valence_pseudodensity
nuclear_Z                   = 53
ecp_removed_electrons       = 28
effective_core_charge       = 25
explicit_electrons          = 25
adt_normalization_N         = 25
atomic_method               = AtomSphAverageRHF
basis                       = def2-TZVPD
ecp                         = exact def2-ECP asset hash
density_parent_hash         = ...
gaussian_fit_hash           = ...
runtime_and_library_hashes  = ...
certified_radial_interval   = ...
audit_D_manifest_hash       = ...
capabilities                = [adt_phi, adt_kappa]
It must explicitly lack:
Copy
Edit code
neutral_full_atom_penetration
The D asset should be separately registered, for example:
Copy
Edit code
element                     = I
role                        = all_electron_radial_audit
representation              = all_electron_relativistic
electron_count              = 53
hamiltonian                 = ...
basis_sequence              = ...
finite_nucleus_model        = ...
spherical_AOC_definition    = ...
density_picture_change      = ...
spin_orbit_audit_hash       = ...
Only a D-derived full-density asset may later supply
Copy
Edit code
role = neutral_full_atom_penetration
Z    = 53
N    = 53
The runtime lookup must be keyed by both 
element and role
. There must be no nearest-element fallback, Br-to-I reuse, atomic-number interpolation, or fallback from 
neutral_full_atom_penetration
 to 
adt_translation_shape
.
7. Reuse of the old non-iodine records
A full rerun is not scientifically required merely because an unused iodine leaf is added to a content-addressed registry.
For record 
i
, define its dependency closure schematically as
h_i = H\!\left( \text{code}, \text{checkpoint}, \text{geometry}, \text{cavity}, \{h_{e,\mathrm{role}}:e\text{ actually used in }i\} \right).
For a record containing no iodine, adding an iodine asset does not change 
h_i
, provided all of the following remain unchanged:
the exact non-iodine coefficient bytes;
the exact non-iodine coefficient bytes;
the operator implementation and arithmetic path;
the operator implementation and arithmetic path;
element/role lookup results;
element/role lookup results;
library/runtime versions;
library/runtime versions;
registry iteration does not influence ordering, seeds, caches, or floating-point reductions;
registry iteration does not influence ordering, seeds, caches, or floating-point reductions;
no global registry hash is used as an input to numerical execution.
no global registry hash is used as an input to numerical execution.
Under those conditions, reuse the old immutable records and issue a new aggregate manifest that references:
each old record’s original receipt and original dependency closure;
each old record’s original receipt and original dependency closure;
the newly computed iodine record and its B/D audit hashes;
the newly computed iodine record and its B/D audit hashes;
an equivalence certificate showing that every non-iodine element/role leaf and operator used by the old records is unchanged.
an equivalence certificate showing that every non-iodine element/role leaf and operator used by the old records is unchanged.
Do 
not
 rewrite the old receipts as though they had been executed under the new provider profile.
If the frozen governance contract defined a single monolithic provider hash as an inseparable part of every row, then a claim that “all 505 rows were executed under one identical monolithic profile” would require a rerun. That is a provenance-policy consequence, not a physical or numerical necessity. The scientifically cleaner solution here is a record-level content-addressed composite manifest, particularly because the existing non-iodine shards are explicitly immutable.
Therefore the additive iodine extension can reuse the 504 old numerical records while changing only the top-level aggregate manifest and adding the newly certified iodine record.
USE ECP-VALENCE ADT WITH ALL-ELECTRON AUDIT
Response actions
