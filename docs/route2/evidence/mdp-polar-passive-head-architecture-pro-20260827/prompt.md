You are reviewing the next model architecture for a quantitative, conservative MACE-MDP + MACE-POLAR implicit-solvation hybrid. I need a hard mathematical and implementation decision, not general suggestions. What is already established Fact 1: The original MACE-MDP permanent q/p + original MACE-POLAR induced source is an operational model but not a common variational functional. The original response has about 46% mean nonuniform induced-MEP error on a 12-molecule matched-QM panel. No experimental solvation target may be used to repair it. Fact 2: The latest relevant preprint is the current Baldwin et al. self-consistent-electrostatics v2 preprint. Its coarse-grained DFT analysis emphasizes reference-density fidelity, first-order-response fidelity, control of higher-order response, explicit applied-field work, Gaussian atomic multipoles, and stable energy-functional models. It also warns about ordinary QEq dissociation and cubic size scaling. Fact 3: The retained physical source/field space is the complete MACE-POLAR two-width radial-GTO l<=1 space: 8 channels per atom, sigma=(1.5,3.0) A. Fixed total charge is handled in a Householder reduced chart of dimension 8N-1. Source-to-MEP and receiver are exact analytic adjoints. Fact 4: Independent QM data are being generated at RKS omegaB97M-V/def2-TZVPD. The frozen split is 32 train / 14 validation / 14 blind unique formulas, grouped by formula and covering H,C,N,O,F,P,S,Cl,Br. No VQM24 energy labels, density coefficients, PCM/cavity quantities, FreeSolv/MNSol identities, or experimental solvation labels are used. Fact 5: Each train molecule has zero-field energy/full exterior MEP/dipole and four geometry-selected exterior point-charge modes at q=+/-1e-3 e. For every perturbed state the stored scalar is the full external enthalpy, including point-charge--nuclear work, plus full molecular MEP and dipole. A pilot closes central enthalpy slope vs full zero-field MEP to 1.91e-7 relative. Numerical energy curvature is deliberately not a target. Fact 6: Sparse interpolation was closed. Each molecule now has a dense PySCF 50-point Lebedev fit frame and an independently frozen rotated audit frame; each partition contains more than 8N exterior probes. No fitted source coefficient is retained. Fact 7: On the first fluorinated molecule (N=11, source dimension 88), fitting only the dense fit partition in the physical 8N radial source span gives: zero MEP fit/audit = 1.12% / 1.47%; response MEP fit/audit = 0.389% / 0.556%; source-point response error = 0.218%; response reciprocity defect after fit = 0.272%; all four symmetric response eigenvalues are negative; the minimum Gram eigenvalue for an SPD Hessian extension is +8.51e-3. A second HCNO pilot gives 1.76% zero-MEP audit and 0.527% response-MEP audit. This strongly suggests the 8N representation is adequate and the old response head is the failure. Fact 8: MAPLE already has a sealed scalar wrapper whose only learned outputs are a permanent reduced-source covector m0(R) and response factor C(R): E(R,xi) = E_vac(R) + m0(R)^T xi - 1/2 ||C(R) xi||^2, so -d2E/dxi2 = C^T C is PSD, and source/JVP/VJP/mixed-coordinate derivatives come only from this scalar graph. It has no trained head yet. Fact 9: Frozen MACE-MDP exposes atomwise q, p, and polarizability tensors that exactly close its molecular dipole/polarizability. Frozen MACE-POLAR zero-field latent/node features may be used as geometry descriptors, but its original source and response cannot be training targets. MACE-MDP and MACE-POLAR backbones must remain frozen. The hard design question Design the minimum viable transferable head for only 32 training molecules that is still structurally correct and can later scale. It must be variable-N, E(3)-equivariant, fixed-charge, smooth in coordinates, passive/reciprocal by construction, and usable with the 8N radial source/field space. It must not become ordinary global QEq with metallic cubic polarizability, and it must not secretly learn per-molecule coefficient labels. Please answer all of the following. Question A: Is the direct reduced-field quadratic scalar above sufficient for the first model, or should the head instead minimize an explicit strongly-convex latent source functional A_theta(R,z)-eta(xi)^T z? Compare expressivity, variable-N implementation, passivity, uniqueness, coordinate forces, and PCM coupling. Make one concrete recommendation for v1. Question B: Give an explicit variable-N parameterization of m0(R) and C(R), or of the recommended latent Hessian. It should exploit frozen MACE-MDP q/p/atomic polarizability and optionally frozen zero-field MACE-POLAR features without treating any original atomic partition as truth. Explain exact charge projection and radial-channel mixing. If using local/pair blocks, prove global PSD/strong convexity and explain why the construction avoids QEq size-scaling and dissociation failures. Question C: Specify the scalar-only training loss from raw E(q), full exterior MEP(q), and molecular dipole(q), with dense fit points used for optimization and rotated audit points used only for model selection. No coefficient loss is allowed. Freeze sensible nondimensionalization and relative weighting so large absolute molecular energy does not dominate field response. Explain how energy gradients generate MEP/source consistently. Question D: With only 32 train / 14 validation molecules, define the smallest justified parameter count, regularization, and ablation sequence. Compare: MDP-only descriptors; MDP plus zero-field POLAR latent features; fixed MDP polarizability response; learned passive correction. State a stop rule that prevents adding neural capacity merely to improve the development set. Question E: Explain how the trained scalar source will couple to reciprocal ddPCM and what extra condition guarantees a unique combined solute-continuum stationary state. Distinguish structural passivity from the combined Hessian/root condition. State the exact first-force path and which coordinate derivatives must remain in the same autograd graph. Question F: Give fail-closed gates for reference MEP, response MEP, energy/MEP conjugacy, charge, reciprocity, passivity, multi-start root, dense audit generalization, rotations, and final held-out electrostatic/full-solvation accuracy. Do not use experimental solvation data for training or model selection. Return equations, tensor shapes, a compact PyTorch-style pseudocode skeleton, and a ranked implementation plan. Critically reject any proposed shortcut that is mathematically incompatible with the evidence above.
