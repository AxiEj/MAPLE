Follow-up Q21 to the previous Pro audit.  We implemented your exact zero-field work-dual diagnostic on seven target-free, already-opened MACE-MDP + MACE-POLAR geometries.  No solvation, MBIS, CDS, radius, scale, or fitted value was used.

Frozen spaces and signs:

* Source s is the original atomwise 4-channel MACE-POLAR q/p source.
* Native receiver u has 8 channels: two potentials and two duplicated Cartesian-gradient blocks.  The learned source occupies the first 1.5-A radial block.
* U embeds an affine potential phi(r)=g dot (r-rbar) into both native radial blocks.  Physical electric field is E=-g.
* C maps source4 to the molecular dipole sum_i(q_i R_i+p_i).
* J is dM_POLAR/du at zero field, B=J U, C_P=C B.
* The exact rectangular physical work map W is the transpose of the fixed source4->radial8 learned-source embedding under the public identity radial pairing.  Thus source work is s^T W u.
* The arithmetic chart G_avg is the atom/radial average of gradient channels only and satisfies G_avg U=I.  It does not mix potential and gradient units.
* MDP supplies only its supervised symmetric molecular alpha_D.

Your Q20 recommendation was the work-dual chart

    G_work = C_P^{-T} B^T W

and symmetric additive tangent correction

    D = -alpha_D - sym(C_P)
    A = C_P^{-1} D
    M_work(u) = M_POLAR(u) + B A G_work u.

Real-checkpoint results across all seven geometries:

* relative skew ||skew(C_P)||_2 / ||sym(C_P)||_2 = 0.00770--0.02490, so the proposed 1e-8 reciprocity gate fails by six orders of magnitude;
* arithmetic-chart work-duality defect ||C_P^T G_avg - B^T W|| / ||B^T W|| = 1.1285--1.2203;
* G_work U-I norm <=1.39e-15;
* total-charge response <=7.29e-17 e;
* symmetric molecular closure error <=1.62e-16;
* correction work-reciprocity defect <=9.15e-16;
* correction magnitude relative to the original uniform source tangent =0.356--0.584.

Thus your strict work-dual candidate fails only the pre-existing C_P skew gate, while its own correction is exactly reciprocal.  The earlier arithmetic additive tangent

    X = C_P^{-1}(-alpha_D)
    M_avg(u)=M_POLAR(u)+B(X-I)G_avg u

instead cancels the entire molecular skew and exactly yields C dM_avg/du U=-alpha_D. It retains charge, POLAR's uniform tangent span, ker(G_avg), and all second/higher source derivatives, but its correction is not generated by the physical work pairing.

The project has two explicitly separate claims:

A. strict common-functional/Tier-V response, which requires work reciprocity;
B. an operational constitutive fixed point with an independently declared scalar ledger and a complete implicit adjoint.  B can produce a conservative nuclear force of that ledger without claiming the electronic response is the ledger gradient.

Please decide this exact fork critically:

1. Do the measured skew and duality defect terminally reject only a work-generated/Tier-V susceptibility correction, or do they also make M_avg scientifically indefensible even as an explicitly nonvariational operational response whose only goal is improved solution-state source prediction? Explain why; do not silently impose Tier-V requirements on B.
2. Is the 1.13--1.22 defect evidence that W is the wrong physical pairing for this 4-source/8-receiver architecture, or is it the expected statement that the arithmetic uniform-coordinate chart is not work-dual? Derive the correct interpretation. Remember that source and receiver dimensions/radial bases are intentionally different in MACE-POLAR.
3. Does the supervised fact that alpha_D is symmetric justify cancelling the observed molecular skew? If yes, derive the unique or minimum-change linear correction K under: C(J+K)U=-alpha_D, charge conservation, range(K) subset range(B), K ker(G_avg)=0, and no change to second/higher derivatives. State the metric needed for “minimum.” If no, identify the missing observable.
4. If M_avg is rejected before any accuracy panel, derive the nearest zero-training alternative that still uses only MDP's molecular alpha and POLAR's atomwise topology. In particular assess an explicit 3D induced-dipole state z with quadratic energy 1/2 z^T alpha_D^{-1} z minus z dot E_eff, lifted into source space through B by a constrained minimum-norm map. Explain how to remove or retain POLAR's native uniform response without double counting, and how it couples to nonuniform ddPCM fields.
5. Give a decisive rule for whether a target-independent finite-field/root audit and then a frozen accuracy panel may be opened for the operational candidate. Separate structural invalidity, operational usefulness, and chemical calibration.

We are not asking for encouragement.  We need a terminal mathematical decision that preserves the operational-vs-Tier-V distinction and identifies the next zero-training implementation if the current tangent is invalid.
