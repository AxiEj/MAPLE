# Verified Chrome Pro review: POLAR-zero permanent + MDP-alpha ADT

Date: 2026-08-19

This directory freezes one genuine Chrome ChatGPT Pro review of the proposed
zero-training hybrid decomposition.  The UI automation record verifies the
exact `Pro` composer pill, the expanded `Pro, 5 of 5.` entry, exact prompt
insertion, visible `Pro thinking`/generation state, the unique prompt/response
boundary, and the complete copied response.  The response terminates with the
required marker `POLAR-ZERO ADT HYBRID DECISION` before its references.

## External adjudication

The review conditionally approves the decomposition for target-independent
operator prequalification:

- MACE-POLAR zero-field point q/p is the permanent source;
- the complete MACE-POLAR zero-field uniform tangent is subtracted from its
  Gaussian response;
- MACE-MDP supplies only its supervised molecular polarizability through ADT;
- this removes linear uniform double counting when `G U = I` and the audited
  potential-gradient sign is used;
- mixed point/Gaussian/ADT sources define a valid role-separated operational
  density and are not intrinsically nonconservative;
- the MDP latent atomwise q/p has no remaining production justification after
  the two disjoint source gates;
- missing independent l>=2 and penetration information is not identifiable
  from the frozen outputs, so failure of this candidate terminates the honest
  zero-training source route.

The answer also identifies a nontrivial approximation: subtracting the full
`J0 U G` tangent removes continuum-visible zero-molecular-dipole response and
replaces it with one ADT allocation constrained only by molecular alpha.  This
must be tested, not described as exact physics.

## Independent local status

The Pro answer is advisory, not evidence by itself.  MAPLE independently locks
`G U = I`, coefficientwise uniform-tangent cancellation, total-charge and ADT
dipole closure, JVP/VJP transpose identities, immutable response/permanent
identity, and real-checkpoint multi-start replay in code/tests.  The first
water canary still fails the inherited lmax/grid objectivity thresholds, so no
accuracy or capability is admitted from this review.
