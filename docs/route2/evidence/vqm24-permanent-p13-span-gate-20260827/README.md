# VQM24 point-P13 permanent source span gate

Status: `fail`.

Every molecule is fit on the frozen 86-point fit frame and evaluated on the rotated audit frame. Coefficients are discarded and are never used as labels. This is a representation gate, not model training.
