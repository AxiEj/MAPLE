# VQM24 radial8 dense observable span gate

Status: `fail`.

Every validation molecule is fit only on its dense fit partition and evaluated on the independent frozen audit partition. No fitted source coefficient is stored or used as a label. This gate tests representation feasibility only; it does not train a head or admit MAPLE capabilities.
