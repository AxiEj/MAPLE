# VQM24 target-independent sector-balanced P13 response modes

All 32 frozen train geometries pass the preregistered geometry-only twelve-mode
construction.  The existing four exterior point-charge modes are retained as
an exact prefix; eight further modes maximize residual row volume after
constant-potential gauge removal and separate radial/l2 RMS balancing.

The largest balanced condition number is `4.3657`, the smallest greedy residual
norm is `0.8788`, and the radial share of selected balanced norm stays between
`0.4864` and `0.5043`.  No QM response, model prediction, PCM/cavity quantity,
or solvation label was read.  This authorizes only a response-generator
prequalification; no additional QM data or model capability exists yet.
