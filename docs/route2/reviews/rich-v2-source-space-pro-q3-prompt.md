You are ChatGPT Pro acting as an independent mathematical and scientific-
software reviewer. Audit a proposed raw-evidence schema correction for a
content-addressed electrostatic E/F revalidation. Be adversarial and distinguish
representation identities from physical kernels.

Frozen implementation facts:

1. The hybrid electronic source space is the registered
   `maple.route2.atomic-l1-source-space.v1` with checkpoint-native real-spherical
   components, in exact order:
   `(net_monopole, real_l1_m0, real_l1_m1, real_l1_m_minus1)`.
   Its source arrays are N x 4.
2. The continuum receiver/native field is a distinct N x 8 two-radial-channel
   representation.
3. The permanent source is `c_MDP` and uses an exterior point-multipole kernel.
4. The responsive checkpoint produces `M_POLAR(R,u)`. The induced source is
   exactly `M_POLAR(R,u*) - M_POLAR(R,0)` and uses the frozen 1.5-A Gaussian
   source kernel.
5. The operational right-hand side is
   `B_point*c_MDP + B_GTO*(M_POLAR(R,u*)-M_POLAR(R,0))`.
6. A proposed `total_source4 = permanent_source4 + induced_source4` is retained
   only as a coefficient-wise charge-audit sum. It must never be passed through
   one common physical kernel. `M_POLAR(R,0)` is a subtraction reference and is
   not added again.
7. Each cold/wide start records its initial-state digest, final N x 8 field,
   final N x 8 actual residual `T(u)-u`, iteration count, convergence flag, and
   final polarization energy. The StateLeaf records total start energy only
   after adding the same frozen vacuum energy exactly once.
8. The legacy root/state digest remains unchanged; the richer StateLeaf has its
   own non-circular digest. No capability opens from this schema alone.
9. The computation seal separately binds canonical `force_panel_contract` and
   `prepared_input_manifest` digests. The latter is derived from the exact
   captured/parsed benzene bytes, canonical H1 radii, frozen water input,
   parent/v1 hashes and force-panel digest; a measurement cannot satisfy this
   by copying two caller-chosen equal hashes.
10. The ordered measurement ledger freezes 141 actual solve occurrences:
    benzene 8 (including the historical second explicit center) and water 133.
    Every event must be consumed exactly once except the deliberately shared
    center reference inside each nine-component Cartesian panel; StateLeaf
    storage deduplication never removes an occurrence.
11. H0 v1 artifacts retain derived forces/errors, maxima and legacy root IDs,
    but not displaced energies, per-start N x 8 fields/residual vectors, or
    per-state N x 4 sources. An exact historical rich tree is therefore not
    identifiable without inventing unobserved leaves. The proposed honest
    replacement is: pin the real H0 v1 object/digest exactly, and separately
    prove the archive-only projection adapter on a labelled deterministic
    synthetic rich golden; neither transfers H0 admission.

Questions:

1. Is N x 4 the category-correct source representation, and is an N x 9 source
   requirement a category error for this exact profile?
2. Is the stated component order sufficient and non-Cartesian, or is another
   basis/convention field needed to eliminate ambiguity?
3. Is the `total_source4` charge-audit sum scientifically legitimate under the
   explicit prohibition on common-kernel use? Recommend a better name/contract
   if needed.
4. Does the permanent/zero-reference/induced decomposition double count or omit
   any source term?
5. Are the per-start residual and energy leaves sufficient to recompute root
   replay/convergence gates without trusting summaries?
6. Identify any remaining category mistake, circular hash, energy double count,
   or evidence-leakage path that must block schema freeze.
7. Give a final `APPROVE` or `BLOCK` verdict with required corrections.
8. Is the proposed replacement in fact 11 scientifically preferable to
   fabricating a historical rich fixture, and may it satisfy the archive-only
   compatibility gate without weakening H1 admission?

Do not recommend zero padding, Cartesian reinterpretation, fitted corrections,
gate relaxation, or capability activation before two clean sealed replays.
End with the exact marker: MAPLE RICH V2 SOURCE SPACE AUDIT COMPLETE
